@isTest
private class TestRestrictContactByName {
/*    @isTest static void addInvalidContact(){
        insert new Contact(LastName='INVALIDNAME');
        for (Contact c : [SELECT Name FROM Contact]){
            System.assert(c.Name != 'INVALIDNAME');
        }
    }
*/
}